# ─────────────────────────────────────────────────────────────────────────────
# CI/CD Scripts — Python Dependencies
# Applies exclusively to ci-cd/scripts/ — NOT to the apileaks/ core package.
#
# Requirements covered:
#   Req 5.1, 5.2  — AWS CodePipeline/CodeBuild pipeline integration
#   Req 5.3        — SSM Parameter Store reads via boto3
#   Req 5.4        — S3 report upload via boto3
#   Req 5.5        — AWS Security Hub publishing via boto3
#   Req 9.4        — PDF generation without external services (reportlab / weasyprint)
# ─────────────────────────────────────────────────────────────────────────────

# ── MFA / TOTP support ────────────────────────────────────────────────────────
# Used by auth_flow.py to generate TOTP codes when APILEAK_LOGIN_MFA_TOTP_SECRET
# is defined (Req 2.7).
# Note: also present in apileaks/requirements.txt at the same pinned version —
# no conflict.
pyotp==2.9.0

# ── YAML parsing ─────────────────────────────────────────────────────────────
# Used by openapi_discoverer.py to parse YAML-format OpenAPI specs (Req 1.4).
# The core package declares PyYAML>=6.0; this tightens the lower bound to
# 6.0.1 (first release with the YAML 1.2 loader improvements) without
# conflicting — any version satisfying >=6.0.1 also satisfies >=6.0.
pyyaml>=6.0.1

# ── JSON Schema validation ────────────────────────────────────────────────────
# Used by openapi_discoverer.py to validate OpenAPI spec structure before
# parsing (Req 1.4).  Not present in the core apileaks/ package — no conflict.
jsonschema==4.21.0

# ── PDF generation (primary) ─────────────────────────────────────────────────
# Used by report_generator.PDFGenerator as the primary rendering backend
# (Req 9.4).  Not present in the core apileaks/ package — no conflict.
reportlab==4.1.0

# ── PDF generation (fallback) ────────────────────────────────────────────────
# Used by report_generator.PDFGenerator when reportlab is unavailable or its
# rendering fails (Req 9.4).  Not present in the core apileaks/ package — no
# conflict.
weasyprint==61.0

# ── AWS SDK ───────────────────────────────────────────────────────────────────
# Used by aws_security_hub.py (BatchImportFindings) and buildspec.yml phases
# that read from SSM Parameter Store and upload to S3 (Req 5.3, 5.4, 5.5).
# Not present in the core apileaks/ package — no conflict.
boto3==1.34.0
