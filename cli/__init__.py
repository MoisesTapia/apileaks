"""APILeak CLI building blocks.

This package holds the CLI-surface helpers (descriptor registry, shared and
module-specific option decorators, execution core) that drive the restructured
``apileaks`` command tree. It contains no OWASP detection logic.
"""
